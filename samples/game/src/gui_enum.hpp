#pragma once

enum GuiMode {
    ModifyTerrainMode,
    DrawTextureMode
};

// if in DrawTextureMode, specify which texture we are currently drawing.
enum DrawTextureType {
    GrassTexture,
    DirtTexture
};
